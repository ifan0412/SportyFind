// app/events/page.tsx
import { PageShell } from "@/components/PageShell";

export default function EventsPage() {
  return (
    <PageShell
      title="Find Matches & Events"
      subtitle="Discover tournaments, leagues, and pickup games in your area."
    />
  );
}